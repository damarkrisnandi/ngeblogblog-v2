---
title: 'Insert Component ke dalam MDX Remote'
description: 'eksperimen untuk memasukkan component ke dalam MDX Remote'
date: 'Tue Jul 02 2024 17:06:46 GMT+0700 (Indochina Time)'
tags: [mdx, component]
---
## Register Component
register component pada ``@/components/main/AppMDXViewer``
```typescript
const components = { 
  Button,
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
  AppCounter 
}
```
 
## ShadCNUI Button
### Code 
call component di **.mdx** file
```
<Button>Click it!</Button>
```
### Result
<Button>Click it!</Button>

## ShadCNUI Card 
### Code
call component di **.mdx** file
```
<Card className="w-3/12">
    <CardHeader>
        <CardTitle>This is Card Title</CardTitle>
        <CardDescription>Lorem ipsum dolor sit amet.</CardDescription>
    </CardHeader>
    <CardContent className="grid gap-4">
        <p className="wrap">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Cras vel bibendum orci.
        </p>
    
    </CardContent>
    <CardFooter>
        <Button className="w-full"> Button at Footer </Button>
    </CardFooter>
</Card>
```

### Result
<Card className="w-3/12">
    <CardHeader>
        <CardTitle>This is Card Title</CardTitle>
        <CardDescription>Lorem ipsum dolor sit amet.</CardDescription>
    </CardHeader>
    <CardContent className="grid gap-4">
        <p className="wrap">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Cras vel bibendum orci.
        </p>
    
    </CardContent>
    <CardFooter>
        <Button className="w-full"> Button at Footer </Button>
    </CardFooter>
</Card>

## Interactive Component
### Code
create component di ``@/components/main/AppCounter``
``` 

'use client'
import { Card, CardContent, CardFooter, } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { useState } from 'react';

const AppCounter = () => {
  const [count, setCount] = useState(0);

  const handleIncrement = () => {
    setCount(count + 1);
  };

  const handleDecrement = () => {
    setCount(count - 1);
  };

  return (
    <Card>
      <CardContent>
        <p>Count: {count}</p>
      </CardContent>
      <CardFooter className='flex space-x-1'>
        <Button onClick={handleDecrement}>Decrement</Button>
        <Button onClick={handleIncrement}>Increment</Button>
      </CardFooter>
    </Card>
  );
};

export default AppCounter;
```
call component di **.mdx** file
```
<AppCounter />
```

### Result
<AppCounter />